services:
  - type: web
    name: mat-sms-pro
    env: node
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: mat-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
